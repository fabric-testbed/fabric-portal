# fabric-portal
FABRIC Portal — a Next.js 15 web app for the FABRIC research testbed. Provides resource discovery, experiment/slice management, project administration, and SSH key handling. Uses CILogon OIDC auth via Nginx + Vouch Proxy. All backend API calls are proxied through server-side route handlers. Built with React 18, Bootstrap 5, and plain JavaScript.
